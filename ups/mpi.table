# This is a dependency only package that simply sets
# up the LSST-provided mpich package.  If you'd like
# to use another MPI implementation, or your own
# install of mpich, just remove the line below and
# otherwise ensure the "mpicc" command is included
# in PATH before installing mpi4py.
setupRequired(mpich)
